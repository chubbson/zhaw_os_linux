#http://git-scm.com/docs/gitignore
#revert git add, before commit
git reset
#discart changes
git stash save --keep-index
git stash drop

#Makefile
../scripts/create-make > makefile
make 
make all
make clean

# run normal
// creating out-lowlevel.txt 
./H0_99_pow_rev 

// sample file reader, with buffering
./Hmisc

// starting HDynFileReader
./H0_99_pow_rev
