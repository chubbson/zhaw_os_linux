#http://git-scm.com/docs/gitignore
#revert git add, before commit
git reset
#discart changes
git stash save --keep-index
git stash drop

#write 
printf("=====HUELP=====\n");
write(STDOUT_FILENO,"=====HELP=====\n",15);
write(STDOUT_FILENO,"q: quit program\n",16);
fflush(stdout);

Output: 
=====HELP=====
it program
=====HUELP=====

Desc: 
printf will be released (flushed) when fflush was called ;)


#Makefile
../scripts/create-make > makefile
make 
make all
make clean

# run normal
./HipcPCD_PCc_CDc_Cx_PDs_DPs


// Hausaufgabe
// PARENT -> CHILD-> DAEMON 
// nach DAEMON. CHILD wird beendet. 
// nach CHILD beendet. 
// PARENT sendet SIGUSR1 an DAEMON
// DAEMON Sendet SIGUSR2 an Parent

// ./HipcPCD_PCc_CDc_Cx_PDs_DPs the same like tellwaitsmaple